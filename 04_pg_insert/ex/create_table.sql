-- create table banners (
--      banner_id int unsigned not null primary key,
--      title varchar(200),
--      url varchar(4000)
-- );
create table banners (
     banner_id bigint not null primary key,
     title varchar(200),
     url varchar(4000)
);
